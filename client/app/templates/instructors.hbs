<h2>Instructors</h2>
{{#data-table}}
	<table class="table">
		<thead>
			<tr>
				<th>Full Name</th>
				<th>Email</th>
				<th>UniqueID</th>
			</tr>
		</thead>
		<tbody>
			{{#each model as |instructor|}}
				<tr>
					<td><strong>{{#link-to 'instructor' instructor}}{{instructor.fullName}}{{/link-to}}</strong></td>
					<td>{{mail-to instructor.email}}</td>
					<td>{{instructor.uniqueid}}</td>
				</tr>
			{{/each}}
			{{outlet}}
		</tbody>
	</table>
{{/data-table}}