import {
  toShortDate
} from 'client/helpers/to-short-date';

module('ToShortDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = toShortDate(42);
  ok(result);
});
